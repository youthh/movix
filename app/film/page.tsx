import React from "react";
import { API_KEY } from "../../Data/Const";
import { movieResponse } from "../../Data/Types/types";

export default async function Film({ id }: { id: string }) {
  return <div>test</div>;
}
